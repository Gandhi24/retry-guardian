[server]
    port         = 8081
    read_timeout  = "5s"
    write_timeout = "10s"

[redis]
    addr          = "localhost:6379"
    password      = ""
    db            = 0
    dial_timeout  = "2s"
    read_timeout  = "2s"
    write_timeout = "2s"

[rules]
    file_path = "rules.yaml"
